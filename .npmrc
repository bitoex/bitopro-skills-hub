# .npmrc

# 預設使用公開 npm registry
registry=https://registry.npmjs.org/

# 如果未來使用私有 registry，取消註解：
# @bitopro:registry=https://npm.internal.bitopro.com/
# //npm.internal.bitopro.com/:_authToken=${NPM_INTERNAL_TOKEN}

# 安全設定
engine-strict=true